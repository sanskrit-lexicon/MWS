# Gap analysis — Round-2 Haiku refresh, 2026-05-28

Post-run audit of [issue #197](https://github.com/sanskrit-lexicon/MWS/issues/197) / branch [`haiku-refresh-2026-05-28`](https://github.com/sanskrit-lexicon/MWS/tree/haiku-refresh-2026-05-28). Twelve gaps identified against the [`/cologne-haiku-refresh` skill](../../../../../../../../Users/user/.claude/commands/cologne-haiku-refresh.md). Sorted: deliverable-visible first, silent omissions second. Each row tracks fix status.

## 🔴 Tier A — Done incorrectly (visible in the issue or branch)

| # | Gap | Fix | Status |
|---|---|---|---|
| 1 | ~~[`SUPPLEMENT_MANIFEST.md`](https://github.com/sanskrit-lexicon/MWS/blob/haiku-refresh-2026-05-28/papers/microanalysis/analysis/SUPPLEMENT_MANIFEST.md) was regenerated by Phase 7 but never made it into commit `ac83d6a`. The branch still shows the old manifest.~~ **False alarm.** Phase 7's regen was content-idempotent — the regenerated bytes matched the manifest already on `docs-pass` (Opus commit `2f7b640` "refresh supplement ZIP manifest post-rebase"). The SHA-256 mismatch between working tree and HEAD is CRLF vs LF; git's content comparison normalizes it (`git status` is clean). The branch's manifest IS current. | None needed. | ☑ (verified 2026-05-28) |
| 2 | Severity miscall: skill rule is `medium if any of H3/H2/H6/H8/H9 ⚠️`. Audit has H8 ⚠️ + H9 ⚠️ → severity should be `medium`, not `minor`. | Edit issue #197 body, change `Severity: minor` → `Severity: medium`. | ☐ |
| 3 | Labels `refresh` and `maintenance` created on MWS at 05:12 UTC unilaterally — skill explicitly said "drop `-l` flags and tell the user the issue is unlabeled" if labels are missing. | Keep both labels (useful), or delete via `gh label delete`. User decides. | ☐ |
| 4 | [`comment_haiku_refresh.md`](https://github.com/sanskrit-lexicon/MWS/blob/haiku-refresh-2026-05-28/mwsissues/haiku_refresh/2026-05-28/comment_haiku_refresh.md) is a byte-identical duplicate of [`ISSUE_COMMENT.md`](https://github.com/sanskrit-lexicon/MWS/blob/haiku-refresh-2026-05-28/mwsissues/haiku_refresh/2026-05-28/ISSUE_COMMENT.md). The skill expected a "repo-internal" variant (older PWG-style name with internal notes), not a copy. | Either delete `comment_haiku_refresh.md`, or rewrite it with internal-only notes (commit SHAs, command lines run, debug info). | ☐ |
| 5 | H10 row in the audit table uses `ℹ️`, which is not in the skill's documented status vocabulary (✅ / ⚠️ only). | Promote to ⚠️ (since the HEAD-checks weren't actually performed) and re-issue the audit. | ☐ |

## 🟡 Tier B — Done incompletely (silent omissions)

| # | Gap | Fix | Status |
|---|---|---|---|
| 6 | RU locale figures were not re-rendered. Only `*-en.{png,svg}` carry mtime 2026-05-28; all `*-ru.*` figures still 2026-05-27. Renderers likely accept a `--locale ru` flag (or similar) that Haiku didn't pass. | Read [`render_heatmap.py`](https://github.com/sanskrit-lexicon/MWS/blob/docs-pass/papers/microanalysis/figures/scripts/render_heatmap.py), find the locale switch, re-run all three renderers for ru, then re-run H4 stamp check. | ☐ |
| 7 | [`render_cross_dict.py`](https://github.com/sanskrit-lexicon/MWS/blob/docs-pass/papers/microanalysis/figures/scripts/render_cross_dict.py) was never invoked. That's why H4 reported `cross-dict-blocks-en.svg` + `cross-dict-density-en.svg` as "missing footer" — those two figures simply weren't re-rendered. | Add `render_cross_dict.py` invocation to Phase 2 of the skill (and run it now for this branch). | ☐ |
| 8 | Three data-export scripts in [`figures/scripts/`](https://github.com/sanskrit-lexicon/MWS/tree/docs-pass/papers/microanalysis/figures/scripts) were skipped entirely: `export_data.py`, `export_dict_blocks.py`, `export_cross_dict.py`. If they produce the `.json` inputs the renderers consume, the figures are coloured-but-stale. | Inspect each script's header docstring, decide whether they belong before Phase 2 (data → figures pipeline). | ☐ |
| 9 | H10 link-rot was punted. Skill says "extracts every `https?://…` match, deduplicates, and HEADs each with a 5s timeout". Haiku counted 138 URLs and stopped — zero actually checked. | Write `link_rot.py` properly (curl HEAD with 5s timeout, parallelise), run against the 13 scoped files, append a real ⚠️ count to the audit. | ☐ |
| 10 | H8 mismatch (manifest=108 vs ZIP=104) never investigated. Haiku speculated "directory entries vs files" without verifying. Could be real data loss. | Diff the manifest entry list vs `unzip -l` output — confirm whether the 4 extras are dirs (benign) or files (data loss). | ☐ |
| 11 | H9 CI failure on [run 26539941482](https://github.com/sanskrit-lexicon/MWS/actions/runs/26539941482) was logged but the failure log itself was never fetched. Could be microanalysis-related or completely unrelated. | `gh run view 26539941482 --log-failed -R sanskrit-lexicon/MWS` and surface the actual error in the issue. | ☐ |
| 12 | The regenerated [`mw-microanalysis-supplementary.zip`](https://github.com/sanskrit-lexicon/MWS/blob/haiku-refresh-2026-05-28/papers/microanalysis/analysis/mw-microanalysis-supplementary.zip) is gitignored — the issue body links to a path that 404s on GitHub. | Either remove the ZIP link from issue body, or `git add -f` the ZIP (breaks the existing gitignore policy — needs user call). | ☐ |

---

## Fix order

Recommended sequence — cheapest first, dependencies later:

1. **#1** manifest commit (one `git add` + amend)
2. **#5** audit-table ℹ️ → ⚠️ (one edit)
3. **#2** severity `minor` → `medium` (one issue-body edit; depends on #5)
4. **#4** delete or rewrite duplicate `comment_haiku_refresh.md`
5. **#11** fetch CI failure log → fold into issue (cheap, no rendering)
6. **#10** investigate ZIP mismatch (diff manifest vs unzip -l)
7. **#7** invoke `render_cross_dict.py`, refresh the two cross-dict figures
8. **#6** find the locale flag, re-render ru figures
9. **#8** decide if export_* scripts need to run; if so, run them and re-render
10. **#9** real HEAD-check on 138 URLs
11. **#12** decide ZIP commit policy with user
12. **#3** keep or remove the labels (cosmetic)

The skill itself (`C:\Users\user\.claude\commands\cologne-haiku-refresh.md`) also needs patches for #6, #7, #8, #9 — but those edits land **after** this run is rectified, so a future invocation doesn't repeat the same omissions.
