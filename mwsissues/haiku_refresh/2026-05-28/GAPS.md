# Gap analysis — Round-2 Haiku refresh, 2026-05-28

Post-run audit of [issue #197](https://github.com/sanskrit-lexicon/MWS/issues/197) / branch [`haiku-refresh-2026-05-28`](https://github.com/sanskrit-lexicon/MWS/tree/haiku-refresh-2026-05-28). Twelve gaps identified against the [`/cologne-haiku-refresh` skill](../../../../../../../../Users/user/.claude/commands/cologne-haiku-refresh.md). Sorted: deliverable-visible first, silent omissions second. Each row tracks fix status.

## 🔴 Tier A — Done incorrectly (visible in the issue or branch)

| # | Gap | Fix | Status |
|---|---|---|---|
| 1 | ~~[`SUPPLEMENT_MANIFEST.md`](https://github.com/sanskrit-lexicon/MWS/blob/haiku-refresh-2026-05-28/papers/microanalysis/analysis/SUPPLEMENT_MANIFEST.md) was regenerated by Phase 7 but never made it into commit `ac83d6a`. The branch still shows the old manifest.~~ **False alarm.** Phase 7's regen was content-idempotent — the regenerated bytes matched the manifest already on `docs-pass` (Opus commit `2f7b640` "refresh supplement ZIP manifest post-rebase"). The SHA-256 mismatch between working tree and HEAD is CRLF vs LF; git's content comparison normalizes it (`git status` is clean). The branch's manifest IS current. | None needed. | ☑ (verified 2026-05-28) |
| 2 | ~~Severity miscall: skill rule is `medium if any of H3/H2/H6/H8/H9 ⚠️`. Audit has H8 ⚠️ + H9 ⚠️ → severity should be `medium`, not `minor`.~~ Corrected in issue #197 body. New rationale names all four ⚠️ rows (H4, H8, H9, H10 after #5 fix) and cites the skill rule. | Done. | ☑ (2026-05-28) |
| 3 | ~~Labels `refresh` and `maintenance` created on MWS at 05:12 UTC unilaterally~~ — process deviation accepted in hindsight. Both labels have sensible names + colours (refresh: `#FEF2C0` light yellow; maintenance: `#BFDADC` light teal) and useful descriptions. Keeping them as part of the MWS label vocabulary going forward. **Skill should be patched** to either ask before creating, or to silently create-if-missing as the default. | Resolved (keep); skill-patch follow-up needed. | ☑ (2026-05-28, keep) |
| 4 | ~~`comment_haiku_refresh.md` is a byte-identical duplicate of `ISSUE_COMMENT.md`. The skill expected a "repo-internal" variant...~~ **False alarm.** Re-reading both skills: cologne-markup-fix says "Save a second copy as `comment_markup_fix.md` (older PWG-style name) for repo-internal reference"; cologne-haiku-refresh says "Save a second copy as `comment_haiku_refresh.md` for repo-internal reference". Both ask for a literal duplicate with an aliased name (naming-convention compat), not a variant. Haiku followed the skill correctly. | None needed. | ☑ (verified 2026-05-28) |
| 5 | ~~H10 row in the audit table uses `ℹ️`, which is not in the skill's documented status vocabulary (✅ / ⚠️ only).~~ Promoted to ⚠️ in both [`haiku_refresh_audit.md`](haiku_refresh_audit.md) and the live issue body (`gh issue edit 197 --body-file …`). Findings prose updated to acknowledge the HEAD-check was skipped rather than "deferred". | Done. | ☑ (2026-05-28) |

## 🟡 Tier B — Done incompletely (silent omissions)

| # | Gap | Fix | Status |
|---|---|---|---|
| 6 | ~~RU locale figures were not re-rendered.~~ Renderers accept `--locale {en,ru}` (default en). Re-ran all three for ru: `heatmap-ru`, `treemap-ru`, `sankey-ru` now stamped at HEAD `05aff86`. Cross-dict ru intentionally deferred per the script's docstring (DOUBTS D11). | Done. | ☑ (2026-05-28) |
| 7 | ~~`render_cross_dict.py` was never invoked.~~ Invoked: `cross-dict-blocks-en` and `cross-dict-density-en` refreshed and stamped. Script's docstring confirms ru is intentionally deferred ("RU deferred until the locale is reviewed, per DOUBTS D11"). The "missing footer" finding was compounded by my verify_stamp.py regex being too strict — cross-dict uses `Source: CDSL DATE` (no `<short>.txt`), and ru uses `Источник:` (Cyrillic). Fixed verifier accepts all three formats. | Done. | ☑ (2026-05-28) |
| 8 | ~~Three data-export scripts were skipped: `export_data.py`, `export_dict_blocks.py`, `export_cross_dict.py`.~~ Ran all three — produced byte-identical output to what was already in `figures/data/*.json` (git status clean after run). Conclusion: scripts are idempotent and only do real work when upstream `mw.txt` at `C:/Users/user/AppData/Local/Temp/mw.txt` is newer than the data JSONs. **Should be added to the skill as a conditional Phase 1.5** that compares mw.txt mtime vs the JSONs and runs only on stale-data. Not critical for this run since the renders we produced are based on current data anyway. | Done (this run); skill addition tracked for next patch. | ☑ (2026-05-28) |
| 9 | ~~H10 link-rot was punted.~~ Wrote `link_rot_v2.py` (HEAD with 5s timeout + GET fallback for 403/405/501, 16 parallel workers). Real result: **73 unique URLs** (Haiku's 138 was un-deduplicated), **6 truly broken + 1 false positive** (a `<code` template-hole in MICROSITE.md the regex caught). Broken URLs: 4 non-existent `sanskrit-lexicon/{armh,abch,acph,acsj}` repos referenced across FIGURES.md / MICROSITE.md / PAPER.md / PAPER_RU.md; one stale `amzn.github.io/style-dictionary/` link in PALETTE.md; one `ivran.ru/east` in PAPER_RU.md. **Audit-only — no edits made.** Full results in [h10_link_rot.log](https://github.com/sanskrit-lexicon/MWS/blob/haiku-refresh-2026-05-28/mwsissues/haiku_refresh/2026-05-28/h10_link_rot.log). | Done. Real link-rot fixes are Sonnet/Opus tier work (per skill); H10 stays audit-only. | ☑ (2026-05-28) |
| 10 | ~~H8 mismatch (manifest=108 vs ZIP=104) never investigated.~~ **False alarm — counting bug in Phase 8.** The grep `^[-*] ` matched four bold section summaries (`- **(root)/** — 9 files`, `- **analysis/** — 24 files`, `- **decisions/** — 8 files`, `- **figures/** — 63 files`) as if they were file entries. Real math: 9+24+8+63 = 104, exactly matches ZIP. Manifest header self-reports "**104 files**". H8 row in audit + issue #197 promoted to ✅. Skill Phase 7 needs the grep replaced with a parse of the "**N files**" line. | Done (results corrected); skill grep patch tracked separately. | ☑ (2026-05-28) |
| 11 | ~~H9 CI failure on [run 26539941482](https://github.com/sanskrit-lexicon/MWS/actions/runs/26539941482) was logged but the failure log itself was never fetched.~~ Log fetched: root cause is a Liquid syntax error in [DATA_DICTIONARY.md:65](https://github.com/sanskrit-lexicon/MWS/blob/master/DATA_DICTIONARY.md#L65) — literal `{%…%}` strings in a markdown table parsed by Jekyll's Liquid engine. **Unrelated to microanalysis.** Issue #197 H9 row + prose updated with the actual error. Side-task spawned to fix DATA_DICTIONARY.md. | Done. | ☑ (2026-05-28) |
| 12 | ~~The regenerated `mw-microanalysis-supplementary.zip` is gitignored — the issue body links to a path that 404s on GitHub.~~ Removed the broken ZIP link from issue #197 (both the Artifacts list and the H8 status link). Replaced with a pointer to [`analysis/.gitignore`](https://github.com/sanskrit-lexicon/MWS/blob/haiku-refresh-2026-05-28/papers/microanalysis/analysis/.gitignore) showing the `*.zip` exclusion policy, plus a one-line "rebuild locally with `python make_supplement.py`" instruction. Repo policy (don't commit build artifacts) preserved. | Done. | ☑ (2026-05-28) |

---

## Fix order

Recommended sequence — cheapest first, dependencies later:

1. **#1** manifest commit (one `git add` + amend)
2. **#5** audit-table ℹ️ → ⚠️ (one edit)
3. **#2** severity `minor` → `medium` (one issue-body edit; depends on #5)
4. **#4** delete or rewrite duplicate `comment_haiku_refresh.md`
5. **#11** fetch CI failure log → fold into issue (cheap, no rendering)
6. **#10** investigate ZIP mismatch (diff manifest vs unzip -l)
7. **#7** invoke `render_cross_dict.py`, refresh the two cross-dict figures
8. **#6** find the locale flag, re-render ru figures
9. **#8** decide if export_* scripts need to run; if so, run them and re-render
10. **#9** real HEAD-check on 138 URLs
11. **#12** decide ZIP commit policy with user
12. **#3** keep or remove the labels (cosmetic)

The skill itself (`C:\Users\user\.claude\commands\cologne-haiku-refresh.md`) also needs patches for #6, #7, #8, #9 — but those edits land **after** this run is rectified, so a future invocation doesn't repeat the same omissions.
